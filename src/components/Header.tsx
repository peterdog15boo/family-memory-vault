/**
 * @deprecated Marketing pages use `MarketingNav` via `MarketingChrome`.
 * Kept as a thin re-export so older imports do not break.
 */
export { MarketingNav as Header } from "@/components/marketing/MarketingNav";

/**
 * @deprecated Marketing pages use `MarketingFooter` via `MarketingChrome`.
 * Kept as a thin re-export so older imports do not break.
 */
export { MarketingFooter as Footer } from "@/components/marketing/MarketingFooter";

/**
 * @deprecated Import from `@/lib/family-tree/genealogy-iq` instead.
 * Re-exports kept so existing imports keep working.
 */

export {
  spouseIdsOf,
  isParentOfChild,
  missingCoParentSpouseIds,
  preferredExistingCoParentId,
  type GenealogyEdge as CoParentEdge,
} from "@/lib/family-tree/genealogy-iq";
